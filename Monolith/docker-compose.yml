services:
  postgres:
    image: postgres:17
    container_name: eventapi_db
    environment:
      POSTGRES_DB: eventapi
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
